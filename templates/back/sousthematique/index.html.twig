{% extends 'base.html.twig' %}

{% block title %}Sousthematique index{% endblock %}

{% block body %}
    <h1>Sousthematique index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>SousTheme</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sousthematique in sousthematiques %}
            <tr>
                <td>{{ sousthematique.id }}</td>
                <td>{{ sousthematique.sousTheme }}</td>
                <td>
                    <a href="{{ path('app_back_sousthematique_show', {'id': sousthematique.id}) }}">show</a>
                    <a href="{{ path('app_back_sousthematique_edit', {'id': sousthematique.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_back_sousthematique_new') }}">Create new</a>
{% endblock %}
