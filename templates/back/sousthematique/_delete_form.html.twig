<form method="post" action="{{ path('app_back_sousthematique_delete', {'id': sousthematique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sousthematique.id) }}">
    <button class="btn">Delete</button>
</form>
