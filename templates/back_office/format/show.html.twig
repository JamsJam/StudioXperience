{% extends 'base.html.twig' %}

{% block title %}Format{% endblock %}

{% block body %}
    <h1>Format</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ format.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ format.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_office_format_index') }}">back to list</a>

    <a href="{{ path('app_back_office_format_edit', {'id': format.id}) }}">edit</a>

    {{ include('back_office/format/_delete_form.html.twig') }}
{% endblock %}
